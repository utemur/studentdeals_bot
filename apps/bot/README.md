# StudentDeals Telegram Bot

Телеграм-бот для авторизации и верификации студенческих почт.

## Функционал

- **Верификация студенческой почты**: отправка 6-значного кода на email через Resend
- **Магическая ссылка**: одноразовая ссылка для доступа к аккаунту на фронтенде
- **Rate limiting**: защита от спама и злоупотреблений
- **Webhook/Polling**: автоматический выбор режима работы

## Технологии

- **TypeScript** - типизированный JavaScript
- **Telegraf** - фреймворк для Telegram ботов
- **Express** - HTTP сервер для webhook
- **tsx** - запуск TypeScript без компиляции
- **tsup** - сборка production бандла

## Установка

```bash
# Установка зависимостей для всего монорепо
pnpm install

# Только для бота
cd apps/bot
pnpm install
```

## Переменные окружения

Создайте `.env` файл в корне проекта со следующими переменными:

```env
# BOT
TELEGRAM_BOT_TOKEN=your_bot_token_from_botfather
BOT_WEBHOOK_SECRET=random_secure_string
BOT_WEBHOOK_BASE=https://your-bot-domain.onrender.com
FRONTEND_URL=https://studentdeals-uz-web.vercel.app
API_URL=https://your-api-domain.onrender.com

# MAIL
RESEND_API_KEY=re_your_api_key

# STUDENT VERIFICATION
STUDENT_EMAIL_DOMAINS=.edu,.ac.uk,.edu.uz
CODE_PEPPER=random_pepper_for_code_hashing

# SECURITY
CODE_TTL_SECONDS=900
CODE_RESEND_COOLDOWN=60
CODE_MAX_ATTEMPTS=5
SESSION_URL_TTL_SECONDS=120

# JWT
JWT_ISSUER=studentdeals
JWT_AUDIENCE=web
JWT_SECRET=your_jwt_secret

# NODE
NODE_ENV=development
```

### Описание переменных

- `TELEGRAM_BOT_TOKEN` - токен бота от @BotFather
- `BOT_WEBHOOK_SECRET` - секретная строка для webhook URL
- `BOT_WEBHOOK_BASE` - базовый URL для webhook (без завершающего слеша)
- `STUDENT_EMAIL_DOMAINS` - список доменов студенческих почт через запятую
- `CODE_PEPPER` - случайная строка для хэширования кодов
- `CODE_TTL_SECONDS` - время жизни кода (по умолчанию 15 минут)
- `CODE_MAX_ATTEMPTS` - максимальное количество попыток ввода кода
- `SESSION_URL_TTL_SECONDS` - время жизни магической ссылки (по умолчанию 2 минуты)

## Разработка

### Локальный запуск (Polling)

```bash
# Из корня монорепо
pnpm -w --filter @studentdeals/bot dev

# Или из директории бота
cd apps/bot
pnpm dev
```

Бот будет работать в режиме polling и автоматически получать обновления от Telegram.

## Production (Webhook)

### 1. Сборка

```bash
pnpm -w --filter @studentdeals/bot build
```

### 2. Установка webhook

```bash
pnpm -w --filter @studentdeals/bot set-webhook
```

Это установит webhook URL на `BOT_WEBHOOK_BASE/webhook/telegram/BOT_WEBHOOK_SECRET`.

### 3. Удаление webhook

```bash
pnpm -w --filter @studentdeals/bot delete-webhook
```

### 4. Запуск

```bash
pnpm -w --filter @studentdeals/bot start
```

## Деплой на Render

### Build команда
```bash
pnpm -w --filter @studentdeals/bot build
```

### Start команда
```bash
node apps/bot/dist/index.js
```

### Environment Variables
Добавьте все переменные окружения из раздела "Переменные окружения".

### Health Check
Бот имеет endpoint `/healthz` для проверки статуса:
```
GET https://your-bot-domain.onrender.com/healthz
```

Ответ: `{"status":"ok"}`

## Структура проекта

```
apps/bot/
├── src/
│   ├── index.ts              # Главный файл, инициализация бота
│   ├── config.ts             # Загрузка конфигурации
│   ├── resend.ts             # Работа с Resend API
│   ├── handlers/
│   │   ├── start.ts          # Обработчик /start
│   │   ├── help.ts           # Обработчик /help
│   │   └── verify.ts         # Flow верификации email
│   └── middlewares/
│       └── rateLimit.ts      # Rate limiting middleware
├── package.json
├── tsconfig.json
└── README.md
```

## Пользовательский flow

1. Пользователь отправляет `/start`
2. Бот показывает клавиатуру с опциями
3. Пользователь нажимает "🎓 Verify student e-mail"
4. Пользователь вводит email
5. API генерирует 6-значный код и отправляет письмо через Resend
6. Пользователь вводит код из письма
7. API проверяет код и создаёт/обновляет пользователя
8. API генерирует магическую ссылку с JWT
9. Бот отправляет кнопку "Open my account" с этой ссылкой

## API контракты

Бот взаимодействует с `apps/api` через следующие endpoints:

### POST /auth/bot/start-email

Запускает процесс верификации email.

**Request:**
```json
{
  "email": "student@university.edu",
  "telegramId": "123456789"
}
```

**Response:**
```json
{
  "verificationId": "cm123abc...",
  "expiresAt": "2024-01-01T12:30:00Z"
}
```

### POST /auth/bot/verify-email

Проверяет введённый код.

**Request:**
```json
{
  "verificationId": "cm123abc...",
  "code": "123456",
  "telegramId": "123456789"
}
```

**Response:**
```json
{
  "ok": true,
  "userId": "cm456def..."
}
```

### POST /auth/bot/issue-session

Генерирует магическую ссылку для доступа к аккаунту.

**Request:**
```json
{
  "telegramId": "123456789"
}
```

**Response:**
```json
{
  "sessionUrl": "https://studentdeals-uz-web.vercel.app/auth/magic?token=eyJhbG..."
}
```

## Безопасность

- **Rate limiting**: максимум 10 команд в минуту на пользователя
- **Хэширование кодов**: коды хранятся в виде SHA256 хэшей
- **TTL кодов**: коды действительны 15 минут
- **Лимит попыток**: максимум 5 попыток ввода кода
- **Одноразовые ссылки**: магические ссылки действительны 2 минуты
- **Секретный webhook**: webhook защищён секретным путём

## Логирование

Бот логирует:
- Запуск в polling/webhook режиме
- Ошибки отправки писем
- Ошибки API запросов
- Общие ошибки бота

Секретные данные (токены, коды) не логируются.

## Поддержка

При возникновении проблем:
1. Проверьте логи бота
2. Проверьте переменные окружения
3. Убедитесь, что API доступен
4. Проверьте webhook установлен (в production режиме)

